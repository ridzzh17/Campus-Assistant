import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF1A73E8);    // campus blue
  static const secondary = Color(0xFF34A853);  // green accent
  static const background = Color(0xFFF5F7FA);
  static const textDark = Color(0xFF202124);
  static const textLight = Color(0xFF5F6368);
}

class AppStrings {
  static const appName = 'Smart Campus Assistant';
  static const loginTitle = 'Welcome Back';
  static const registerTitle = 'Create Account';
}

class AppConfig {
  static const cloudinaryCloudName = 'di5nziauv';
  static const cloudinaryUploadPreset = 'campus';
}